import { StyleSheet } from 'react-native';

export const HeadBarStyle = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    position: 'absolute',
  },
  headerIcon: {
    position: 'absolute',
    top: 16,
    left: 16,
  },
});

import { StyleSheet } from 'react-native';

export const MfcTextSizes = StyleSheet.create({
  large: {
    fontSize: 16,
    lineHeight: 16 * 1.4,
  },
  normal: {
    fontSize: 14,
    lineHeight: 14 * 1.4,
  },
  small: {
    fontSize: 12,
    lineHeight: 12 * 1.4,
  },
});
